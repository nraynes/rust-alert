[package]
name = "alert-test"
version = "0.3.0"
edition = "2024"
license  = "MIT OR Apache-2.0"
description = "A crate for testing the alert macro from rust-alert."
publish = false

[dependencies]
rust-alert = { path = "../alert-macro" }
