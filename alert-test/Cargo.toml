[package]
name = "alert-test"
version = "0.0.0"
edition = "2024"
description = "A crate for testing the alert macro from rust-alert."
publish = false

[dependencies]
rust-alert = { path = "../alert-macro" }
